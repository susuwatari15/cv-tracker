
# Optional: restrict which hosts a user-configured AI endpoint may point at.
# Comma-separated hostnames; matching is exact-or-subdomain.
# Leave unset to allow any host that passes URL validation (fine for local dev,
# recommended to set when deploying for more than one user).
# AI_ENDPOINT_ALLOWLIST=openai.com,gateway.corp.internal
